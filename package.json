{
  "name": "monorepo-test",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "dev:frontend": "yarn workspace @monorepo-test/frontend run dev"
  }
}
